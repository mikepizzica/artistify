<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Artistify</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class = "bg-sky-100 body">
  <div class="flex-column justify-space-around font-medium px-3 py-2 rounded-lg hover:text-gray-900">
    <header class="flex justify-evenly align-center p-2 wrapper">
      <h1 class ="stage">
        <div class ="cube bg-blue-200 font-mono text-base rounded-none">
        <a href="/" class="face face_1 bg-white hover:text-yellow-600 rounded-none border-2 border-black">Artistify</a>
        <a href="/" class="face face_2 bg-white hover:text-blue-600 rounded-none border-2 border-black">Artistify</a>
        <a href="/" class="face face_3 bg-white hover:text-green-600 rounded-none border-2 border-black">Artistify</a>
        <a href="/" class="face face_4 bg-white hover:text-pink-600 rounded-none border-2 border-black">Artistify</a>
        </div>
      </h1>
      <nav class="bg-white shadow-lg text-center row-auto">
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" class="font-medium px-3 py-2 text-gray-700 rounded-lg hover:bg-gray-100 hover:text-gray-900">profile</a> |
        <button class="no-button font-medium px-3 py-2 text-gray-700 rounded-lg hover:bg-gray-100 hover:text-white-900" id="logout">logout</button>
        {{else}}
        <a href="/login"><button class="no-button font-medium px-3 py-2 text-gray-700 hover:bg-gray-100 hover:text-gray-900">login</button></a>
        {{/if}}
      </nav >
    </header>

    <div class="container" >
    <main class="p-5 columns flex flex-col justify-center">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    </div>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
